# Admin AI Agent

Protected endpoint: `POST /ai-admin-agent/chat`

The agent loads live business data for products, customers, orders, inventory, batches, expenses, deliveries, riders, referrals, analytics, and conversations. It can use approved application tools. Consequential writes require explicit administrator confirmation.

Admin UI: `/settings/ai-agent`.
