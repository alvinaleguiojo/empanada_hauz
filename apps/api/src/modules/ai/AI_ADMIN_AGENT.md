# Admin AI Agent

The protected Admin AI Agent is available at `POST /ai-admin-agent/chat` and the Admin Settings UI is available at `/settings/ai-agent`.

It loads live business data for products, customers, orders, inventory, batches, expenses, deliveries, riders, referrals, analytics, and conversations, then uses approved application tools for current operational questions.

Consequential write actions require explicit administrator confirmation.
