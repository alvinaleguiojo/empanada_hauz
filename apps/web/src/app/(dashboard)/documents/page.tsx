"use client";

import { ChangeEvent, useEffect, useMemo, useState } from "react";
import { FileText, Folder, FolderPlus, Grid2X2, List, MoreVertical, Search, Trash2, Upload, Download, Star, Home, HardDrive, Users } from "lucide-react";
import { apiFetch } from "@/lib/api";
import { API_URL } from "@/lib/config";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";

type DocumentItem = { _id: string; name: string; type: "file" | "folder"; mimeType: string | null; size: number; folderId: string | null; public?: boolean; url?: string; source?: string | null; isSystem?: boolean; ownerName?: string | null; starred?: boolean; createdAt: string; updatedAt: string };
const MAX_FILE_SIZE = 10 * 1024 * 1024;
function formatBytes(size: number) { if (size < 1024) return `${size} B`; if (size < 1024 * 1024) return `${(size / 1024).toFixed(1)} KB`; if (size < 1024 * 1024 * 1024) return `${(size / (1024 * 1024)).toFixed(1)} MB`; return `${(size / (1024 * 1024 * 1024)).toFixed(1)} GB`; }
function isImage(item: DocumentItem) { return item.type === "file" && Boolean(item.mimeType?.startsWith("image/")); }
function isPdf(item: DocumentItem) { return item.type === "file" && item.mimeType === "application/pdf"; }

export default function DocumentsPage() {
  const [items, setItems] = useState<DocumentItem[]>([]), [folderId, setFolderId] = useState<string | null>(null), [folderTrail, setFolderTrail] = useState<DocumentItem[]>([]), [search, setSearch] = useState(""), [view, setView] = useState<"grid" | "list">("grid"), [section, setSection] = useState<"drive" | "system" | "shared" | "starred">("drive"), [loading, setLoading] = useState(true), [uploading, setUploading] = useState(false), [notice, setNotice] = useState<string | null>(null);
  const endpoint = useMemo(() => { const p = new URLSearchParams(); if (folderId) p.set("folderId", folderId); if (search) p.set("search", search); if (section === "system") p.set("system", "true"); if (section === "shared") p.set("shared", "true"); if (section === "starred") p.set("starred", "true"); return `/documents?${p.toString()}`; }, [folderId, search, section]);
  async function load() { setLoading(true); try { setItems(await apiFetch<DocumentItem[]>(endpoint)); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to load documents."); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, [endpoint]);
  function resetNavigation(next: typeof section) { setSection(next); setFolderTrail([]); setFolderId(null); }
  async function createFolder() { const name = window.prompt("Folder name"); if (!name?.trim()) return; try { await apiFetch("/documents/folder", { method: "POST", body: JSON.stringify({ name: name.trim(), folderId }) }); await load(); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to create folder."); } }
  async function uploadFiles(event: ChangeEvent<HTMLInputElement>) { const files = Array.from(event.target.files ?? []); event.target.value = ""; if (!files.length) return; setUploading(true); setNotice(null); try { for (const file of files) { if (file.size > MAX_FILE_SIZE) throw new Error(`${file.name} is larger than 10 MB.`); const dataUrl = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = () => reject(new Error(`Unable to read ${file.name}.`)); reader.readAsDataURL(file); }); await apiFetch("/documents", { method: "POST", body: JSON.stringify({ name: file.name, mimeType: file.type || "application/octet-stream", size: file.size, dataUrl, folderId, public: file.type.startsWith("image/"), source: "documents-page", isSystem: false }) }); } setNotice(`${files.length} file${files.length === 1 ? "" : "s"} uploaded.`); await load(); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to upload files."); } finally { setUploading(false); } }
  async function remove(item: DocumentItem) { if (item.isSystem) { setNotice("System documents are read-only."); return; } if (!window.confirm(`Delete ${item.name}?`)) return; try { await apiFetch(`/documents/${item._id}`, { method: "DELETE" }); await load(); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to delete file."); } }
  async function openFile(item: DocumentItem) { if (item.type !== "file" || !item.url) return; if (item.public) { window.open(`${API_URL}${item.url}`, "_blank", "noopener,noreferrer"); return; } try { const token = window.localStorage.getItem("empanada-token"); const response = await fetch(`${API_URL}/documents/${item._id}/download`, { headers: token ? { authorization: `Bearer ${token}` } : {} }); if (!response.ok) throw new Error(await response.text()); const blobUrl = URL.createObjectURL(await response.blob()); window.open(blobUrl, "_blank", "noopener,noreferrer"); window.setTimeout(() => URL.revokeObjectURL(blobUrl), 60000); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to open file."); } }
  function openFolder(item: DocumentItem) { if (item.type !== "folder") return; setFolderTrail((current) => [...current, item]); setFolderId(item._id); }
  function goHome() { setFolderTrail([]); setFolderId(null); }
  function goTrail(index: number) { const next = folderTrail.slice(0, index + 1); setFolderTrail(next); setFolderId(next[next.length - 1]?._id ?? null); }
  const counts = useMemo(() => ({ folders: items.filter(i => i.type === "folder").length, files: items.filter(i => i.type === "file").length }), [items]);
  const nav = [
    ["drive", HardDrive, "My Drive"], ["system", Home, "System Documents"], ["shared", Users, "Shared with me"], ["starred", Star, "Starred"],
  ] as const;
  return <main className="mx-auto flex w-full max-w-[1500px] gap-6 px-5 py-7 lg:px-8">
    <aside className="hidden w-56 shrink-0 md:block"><div className="sticky top-6 space-y-1"><Button variant="secondary" className="mb-4 w-full justify-start gap-2 rounded-xl" onClick={() => document.getElementById("document-upload")?.click()}><Upload size={16} /> New upload</Button>{nav.map(([key, Icon, label]) => <button key={key} onClick={() => resetNavigation(key)} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${section === key ? "bg-accent/10 font-semibold text-accent" : "text-foreground/60 hover:bg-white/[0.04] hover:text-foreground"}`}><Icon size={17} /> {label}</button>)}</div></aside>
    <section className="min-w-0 flex-1"><div className="mb-6 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-sm text-foreground/45">Central storage for system files and user uploads.</p><h1 className="text-3xl font-semibold tracking-tight">Documents</h1><p className="mt-2 text-sm text-foreground/55">A shared Drive-style library for Empanada Hauz.</p></div><div className="flex flex-wrap gap-2"><Button variant="secondary" onClick={createFolder}><FolderPlus size={16} /> New folder</Button><label className={`inline-flex h-10 cursor-pointer items-center justify-center gap-2 rounded-lg bg-[linear-gradient(135deg,rgb(var(--accent)),#ff8a4d)] px-4 text-sm font-semibold text-white shadow-[0_14px_34px_rgb(var(--accent)/0.24)] ${uploading ? "pointer-events-none opacity-55" : ""}`}><Upload size={16} /> {uploading ? "Uploading…" : "Upload"}<input id="document-upload" type="file" multiple className="sr-only" onChange={uploadFiles} /></label></div></div>
    {notice ? <div className="mb-5 rounded-lg border border-accent/20 bg-accent/10 px-4 py-3 text-sm">{notice}</div> : null}
    <Card className="mb-5 p-3"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="flex min-w-0 items-center gap-2 overflow-x-auto whitespace-nowrap text-sm text-foreground/55"><button type="button" onClick={goHome} className="font-medium text-foreground hover:text-accent">{section === "system" ? "System Documents" : section === "shared" ? "Shared with me" : section === "starred" ? "Starred" : "My Drive"}</button>{folderTrail.map((item, index) => <span key={item._id} className="flex items-center gap-2"><span>/</span><button type="button" onClick={() => goTrail(index)} className="hover:text-foreground">{item.name}</button></span>)}</div><div className="flex flex-1 items-center gap-2 lg:justify-end"><div className="relative min-w-0 flex-1 lg:max-w-md"><Search size={15} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-foreground/35" /><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search in Documents" className="w-full rounded-xl border border-white/[0.1] bg-background px-9 py-2.5 text-sm outline-none focus:border-accent/50" /></div><div className="flex rounded-xl border border-white/[0.1] p-1"><button type="button" onClick={() => setView("grid")} className={`rounded-lg p-2 ${view === "grid" ? "bg-white/[0.1]" : ""}`} aria-label="Grid view"><Grid2X2 size={16} /></button><button type="button" onClick={() => setView("list")} className={`rounded-lg p-2 ${view === "list" ? "bg-white/[0.1]" : ""}`} aria-label="List view"><List size={16} /></button></div></div></div></Card>
    <div className="mb-4 flex items-center justify-between text-xs text-foreground/40"><span>{counts.folders} folder{counts.folders === 1 ? "" : "s"} · {counts.files} file{counts.files === 1 ? "" : "s"}</span>{folderId ? <button type="button" onClick={goHome} className="hover:text-foreground">Back to Drive</button> : null}</div>
    {loading ? <Card className="p-10 text-center text-sm text-foreground/45">Loading documents…</Card> : items.length === 0 ? <Card className="p-12 text-center"><Folder className="mx-auto h-10 w-10 text-foreground/20" /><h2 className="mt-4 font-semibold">No documents here</h2><p className="mt-1 text-sm text-foreground/45">Upload a file or create a folder to get started.</p></Card> : view === "grid" ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-5">{items.map(item => <Card key={item._id} className="group relative overflow-hidden p-0 transition hover:-translate-y-0.5 hover:border-accent/30"><button type="button" onClick={() => item.type === "folder" ? openFolder(item) : void openFile(item)} className="block w-full text-left"><div className="flex aspect-[4/3] items-center justify-center overflow-hidden bg-white/[0.03]">{isImage(item) ? <img src={item.public ? `${API_URL}${item.url}` : item.url} alt={item.name} className="h-full w-full object-cover" /> : item.type === "folder" ? <Folder className="h-14 w-14 text-accent/80" /> : <FileText className="h-14 w-14 text-foreground/30" />}</div><div className="p-4"><div className="flex items-center gap-2"><p className="truncate text-sm font-semibold" title={item.name}>{item.name}</p>{item.isSystem ? <span className="shrink-0 rounded-full bg-accent/10 px-2 py-0.5 text-[10px] font-semibold text-accent">SYSTEM</span> : null}</div><p className="mt-1 text-xs text-foreground/40">{item.type === "folder" ? "Folder" : isPdf(item) ? "PDF" : item.mimeType || "File"}{item.type === "file" ? ` · ${formatBytes(item.size)}` : ""}</p></div></button>{item.type === "file" ? <button type="button" onClick={() => void openFile(item)} className="absolute right-2 top-2 rounded-lg border border-white/[0.1] bg-black/60 p-2 text-white opacity-0 transition group-hover:opacity-100" aria-label="Open"><Download size={15} /></button> : null}{item.isSystem ? null : <button type="button" onClick={() => void remove(item)} className="absolute bottom-2 right-2 rounded-lg bg-black/55 p-2 text-white opacity-0 transition group-hover:opacity-100" aria-label="Delete"><Trash2 size={14} /></button>}</Card>)}</div> : <Card className="overflow-hidden p-0"><div className="grid grid-cols-[minmax(0,1fr)_140px_110px_110px_56px] border-b border-white/[0.07] px-4 py-3 text-xs uppercase tracking-[0.14em] text-foreground/35"><span>Name</span><span>Owner</span><span>Type</span><span>Size</span><span /></div>{items.map(item => <div key={item._id} className="grid grid-cols-[minmax(0,1fr)_140px_110px_110px_56px] items-center border-b border-white/[0.05] px-4 py-3 last:border-b-0"><button type="button" onClick={() => item.type === "folder" ? openFolder(item) : void openFile(item)} className="flex min-w-0 items-center gap-3 text-left"><span className="flex h-9 w-9 shrink-0 items-center justify-center overflow-hidden rounded-lg bg-white/[0.04]">{isImage(item) ? <img src={item.public ? `${API_URL}${item.url}` : item.url} alt="" className="h-full w-full object-cover" /> : item.type === "folder" ? <Folder size={18} className="text-accent" /> : <FileText size={18} className="text-foreground/35" />}</span><span className="truncate text-sm font-medium">{item.name}</span>{item.isSystem ? <span className="rounded-full bg-accent/10 px-2 py-0.5 text-[10px] font-semibold text-accent">SYSTEM</span> : null}</button><span className="truncate text-xs text-foreground/45">{item.isSystem ? "System" : item.ownerName || "You"}</span><span className="text-xs text-foreground/45">{item.type === "folder" ? "Folder" : item.mimeType || "File"}</span><span className="text-xs text-foreground/45">{item.type === "folder" ? "—" : formatBytes(item.size)}</span>{item.isSystem ? <span /> : <button type="button" onClick={() => void remove(item)} className="justify-self-end rounded-lg p-2 text-foreground/40 hover:bg-danger/10 hover:text-danger" aria-label={`Delete ${item.name}`}><MoreVertical size={16} /></button>}</div>)}</Card>}
    </section>
  </main>;
}
